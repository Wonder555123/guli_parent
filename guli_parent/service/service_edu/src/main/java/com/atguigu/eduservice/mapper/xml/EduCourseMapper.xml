<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.eduservice.mapper.EduCourseMapper">

<!--    根据课程id,查询课程确定信息-->
    <select id="getPublishInfo" parameterType="String" resultType="com.atguigu.eduservice.entity.vo.CoursePublishVo">
        SELECT ec.id,ec.title,ec.price,ec.cover,ec.lesson_num as lessonNum,
               et.name as teacherName,
               es1.title osubjectLevelOne,
               es2.title subjectLevelTwo
        FROM edu_course ec
                 LEFT JOIN edu_course_description ecd ON  ec.id = ecd.id
                 LEFT JOIN edu_teacher et ON ec.teacher_id = et.id
                 LEFT JOIN edu_subject es1 ON ec.subject_parent_id = es1.id
                 LEFT JOIN edu_subject es2 ON ec.subject_id = es2.id
        WHERE ec.id = #{courseId}
    </select>

    <select id="getBaseCourseInfo" parameterType="String" resultType="com.atguigu.eduservice.frontVo.CourseWebVo">
        SELECT ec.id,ec.title,ec.price,ec.cover,ec.lesson_num as lessonNum,
               ec.buy_count as buyCount,
               ec.view_count as viewCount,
               ecd.description,
               et.id as teacherId,
               et.intro,
               et.avatar,
               et.name as teacherName,
               es1.id as  subjectLevelOneId,
               es2.id as subjectLevelTwoId,
               es1.title as subjectLevelOne,
               es2.title as subjectLevelTwo
        FROM edu_course ec
                 LEFT JOIN edu_course_description ecd ON  ec.id = ecd.id
                 LEFT JOIN edu_teacher et ON ec.teacher_id = et.id
                 LEFT JOIN edu_subject es1 ON ec.subject_parent_id = es1.id
                 LEFT JOIN edu_subject es2 ON ec.subject_id = es2.id
        WHERE ec.id = #{courseId}
    </select>


</mapper>
